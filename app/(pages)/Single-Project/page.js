'use client'

import React from 'react'
import { useRouter } from 'next/navigation'
import projectList from '../projects/projectList'

const page = () => {
    // const router = useRouter();
    // const { id } = router.query;
  
    // const getProjectDetailsById = (projectId) => {
    //   return projectList.find((project) => project.id === projectId);
    // };
  
    // const projectDetails = getProjectDetailsById(id);
  
    // if (!projectDetails) {
    //   return <p>Loading...</p>;
    // }
  
    // const { title, technologies, description } = projectDetails;

  return (
    <div>
        {/* <h1>{title}</h1>
        <p>{technologies}</p>
        <p>{description}</p> */}
    </div>
  )
}

export default page